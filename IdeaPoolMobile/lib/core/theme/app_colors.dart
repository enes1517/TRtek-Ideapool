import 'package:flutter/material.dart';

class AppColors {
  // Kurumsal Renk Paleti
  static const Color primary = Color(0xFF0A2540);     // Deep Navy Blue
  static const Color primaryLight = Color(0xFF1E3A5F); // Lighter Navy
  static const Color accent = Color(0xFF20C997);      // Teal
  
  // Arkaplan & Yüzey Renkleri
  static const Color background = Color(0xFFF8F9FA);  // Light Gray
  static const Color surface = Colors.white;          // White Surface
  static const Color backgroundDark = Color(0xFF0F172A); // Dark Mode Background
  static const Color surfaceDark = Color(0xFF1E293B);    // Dark Mode Surface

  // Yazı Renkleri
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textLight = Colors.white;

  // Durum Renkleri (Badges)
  static const Color success = Color(0xFF10B981);     // Emerald Green
  static const Color error = Color(0xFFEF4444);       // Coral Red
  static const Color warning = Color(0xFFF59E0B);     // Amber
  
  // Kenarlık & Çizgi
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFF334155);
}
